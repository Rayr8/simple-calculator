//Addition
function add(x, y) {
    let sum = x + y;
    console.log(sum);
}

add(5, 3);

//Subtraction
function subtract(x, y) {
    let subtract = x - y;
    console.log(subtract);
}

subtract(10, 3);

//Multiplication
function multiply(x, y) {
    let multiply = x * y;
    comsole.log(multiply);
}

multiply(5, 2);

//Division 
function divide(x, y) {
    let divide = x / y;
    console.log(divide);
}
 divide(20, 10);

 
